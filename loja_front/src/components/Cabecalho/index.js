import React from 'react'
import './styles.css'

const Cabecalho = () => {
  return (
    <header class="header-container" id="container">
      <button>login</button>
    </header>
  );
};

export default Cabecalho;